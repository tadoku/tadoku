bazel-*

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
/bin
/tmp

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Don't commit the environment variables that are in use
.env
.env.local
tilt_config.json
.tools/

# Keep retired Tilt artifacts ignored in existing checkouts.
.helm/
.tilt/
tilt_modules/
/api/

node_modules/
.claude

# Environment-specific dev-cli settings and rendered manifests.
.dev/config.json
.dev/local/
