# Tadoku Paper assets

This directory contains the distributable, framework-independent font and
brand sources for Paper. See the child README files for geometry, upstream
commits, licenses, and reproduction details.

Verify every production asset from this directory with:

```sh
sha256sum --check SHA256SUMS
```

The manifest intentionally excludes documentation files so prose-only updates
do not change production-asset integrity records.
