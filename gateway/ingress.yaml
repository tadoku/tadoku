apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: tadoku
  annotations:
    kubernetes.io/ingress.class: kong
    konghq.com/strip-path: "true"
spec:
  rules:
  - http:
      paths:
      - path: /tadoku-contest-api
        backend:
          serviceName: tadoku-contest-api
          servicePort: 4000
      - path: /identity-api
        backend:
          serviceName: identity-api
          servicePort: 4000
