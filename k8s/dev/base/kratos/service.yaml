apiVersion: v1
kind: Service
metadata:
  name: kratos-public
spec:
  selector: {app: kratos}
  ports: [{name: http, port: 80, targetPort: 4433}]
---
apiVersion: v1
kind: Service
metadata:
  name: kratos-admin
spec:
  selector: {app: kratos}
  ports: [{name: http, port: 80, targetPort: 4434}]
