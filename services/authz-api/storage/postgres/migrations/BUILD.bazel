load("@rules_pkg//:pkg.bzl", "pkg_tar")

pkg_tar(
    name = "tar",
    srcs = glob(["**/*.sql"]),
    mode = "0644",
    package_dir = "/migrations",
    visibility = ["//services/authz-api:__pkg__"],
)
