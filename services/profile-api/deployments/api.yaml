apiVersion: v1
kind: Namespace
metadata:
  name: tdk-profile-api
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: profile-api
  namespace: tdk-profile-api
  labels:
    app: profile-api
spec:
  selector:
    matchLabels:
      app: profile-api
  template:
    metadata:
      labels:
        app: profile-api
    spec:
      serviceAccountName: profile-api
      initContainers:
        - name: migrations
          image: profile-api-migrate-image
          command: ['/migrate']
          args: ['-source', 'file:///migrations', '-database', '$(DATABASE_URL)', 'up']
          env:
          - name: DATABASE_URL
            value: "postgres://profile:foobar@postgres-profile.default/profile?sslmode=disable"
      containers:
      - name: profile-api
        image: profile-api-image
        ports:
        - containerPort: 8000
        env:
          - name: API_POSTGRES_URL
            value: "postgres://profile:foobar@postgres-profile.default/profile?sslmode=disable"
          - name: API_PORT
            value: "8000"
          - name: API_JWKS
            value: "http://oathkeeper-api.default:4456/.well-known/jwks.json"
          - name: API_KETO_READ_URL
            value: "http://keto-read.default:4466"
          - name: API_KRATOS_URL
            value: "http://kratos-admin.default"
          - name: API_OATHKEEPER_URL
            value: "http://oathkeeper-proxy.default:4455"
          - name: SERVICE_NAME
            value: "profile-api"
        volumeMounts:
        - name: k8s-token
          mountPath: /var/run/secrets/tokens
          readOnly: true
        readinessProbe:
          httpGet:
            scheme: HTTP
            path: /readyz
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 3
        livenessProbe:
          httpGet:
            scheme: HTTP
            path: /livez
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 3
      volumes:
        - name: k8s-token
          projected:
            sources:
              - serviceAccountToken:
                  path: token
                  expirationSeconds: 3600
                  audience: profile-api
---
apiVersion: v1
kind: Service
metadata:
  name: profile-api
  namespace: tdk-profile-api
  labels:
    app: profile-api
    service: profile-api
spec:
  ports:
  - port: 80
    targetPort: 8000
    name: http
  selector:
    app: profile-api
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: profile-api
  namespace: tdk-profile-api
