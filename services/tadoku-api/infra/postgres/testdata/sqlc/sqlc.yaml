version: "2"
sql:
  - engine: postgresql
    schema: schema.sql
    queries: queries.sql
    gen:
      go:
        package: pgxcompat
        out: ../../internal/pgxcompat
        sql_package: pgx/v5
        emit_empty_slices: true
