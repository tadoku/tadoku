version: "2"
sql:
  - engine: postgresql
    schema: ../../migrations
    queries:
      - scoring.sql
    gen:
      go:
        package: scoring
        out: ../../generated/sqlc/scoring
        sql_package: pgx/v5
        emit_empty_slices: true
        omit_unused_structs: true
