apiVersion: batch/v1
kind: Job
metadata:
  name: seed-tadoku-contest-api-job
spec:
  template:
    metadata:
      name: "seed-tadoku-contest-api-job-pod"
    spec:
      restartPolicy: Never
      containers:
      - name: seed-tadoku-contest-api
        imagePullPolicy: IfNotPresent
        image: postgres:12
        command: [ "bin/sh", "-c", "psql -a -f /seed.sql" ]
        volumeMounts:
          - name: seed-sql
            mountPath: /seed.sql
            subPath: seed.sql
        envFrom:
          - secretRef:
              name: tadoku-user.tadoku-contest-api-db.credentials.postgresql.acid.zalan.do
        env:
        - name: PGUSER
          valueFrom:
            secretKeyRef:
              name: tadoku-user.tadoku-contest-api-db.credentials.postgresql.acid.zalan.do
              key: username
        - name: PGPASSWORD
          valueFrom:
            secretKeyRef:
              name: tadoku-user.tadoku-contest-api-db.credentials.postgresql.acid.zalan.do
              key: password
        - name: PGHOST
          value: tadoku-contest-api-db
        - name: PGDATABASE
          value: tadoku
      volumes:
        - name: seed-sql
          configMap:
            name: seed-sql-tadoku-contest-api
